{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run shared:build && npm run prisma:generate && npm run build --workspace @sbl/backend"
  },
  "deploy": {
    "startCommand": "npm run prisma:migrate:deploy && npm run start --workspace @sbl/backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
