{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "nixpacksConfigPath": "/src/backend/railway.json",
    "watchPatterns": ["/src/backend/**", "/src/shared/**"],
    "buildCommand": "npm install && npm run shared:build && npm run prisma:generate --workspace @sbl/backend && npm run build --workspace @sbl/backend"
  },
  "deploy": {
    "startCommand": "npm run prisma:migrate:deploy --workspace @sbl/backend && npm run start --workspace @sbl/backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
