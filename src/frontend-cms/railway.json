{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "nixpacksConfigPath": "/src/frontend-cms/railway.json",
    "watchPatterns": ["/src/frontend-cms/**", "/src/shared/**"],
    "buildCommand": "npm install && npm run shared:build && npm run build --workspace @sbl/frontend-cms"
  },
  "deploy": {
    "staticPublishPath": "src/frontend-cms/dist"
  }
}
